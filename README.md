# Telegram-бот для аналитики видео (ТПШ ДВФУ)

Асинхронный Telegram-бот, который переводит запросы пользователя на естественном языке в SQL-запросы, выполняет их в базе данных PostgreSQL и возвращает точный числовой результат.

## 🛠 Технологический стек
- **Python 3.10**
- **aiogram 3** — для асинхронного взаимодействия с Telegram API.
- **asyncpg** — для быстрого асинхронного доступа к PostgreSQL.
- **PostgreSQL 15** — реляционная база данных для хранения статистики.
- **Groq API (Llama-3)** — LLM для генерации SQL-запросов.
- **Docker & Docker Compose** — для контейнеризации и запуска в одну команду.

## 🚀 Как запустить проект локально

1. Склонируйте репозиторий:
   ```bash
   git clone <ссылка_на_ваш_репозиторий>
   cd <имя_папки>